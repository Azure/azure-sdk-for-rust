[package]
name = "azure_canary"
version = "0.2.0"
description = "Template crate for Azure SDK pipeline testing"
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
azure_canary_core = { path = "../azure_canary_core", version = "0.2.0" }
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
