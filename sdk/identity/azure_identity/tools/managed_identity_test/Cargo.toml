[package]
name = "managed_identity_test"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
azure_identity = { path = "../../" }
azure_storage_blob = { path = "../../../../storage/azure_storage_blob" }
azure_core = { path = "../../../../core/azure_core" }
tokio = { version = "1.0", features = ["full"] }
url = "2.5"
axum = "0.7"
serde = { version = "1.0", features = ["derive"] }
