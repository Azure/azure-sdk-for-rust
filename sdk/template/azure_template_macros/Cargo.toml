[package]
name = "azure_template_macros"
version = "0.1.0"
description = "Proc macros for Azure Template SDK"
edition = "2021"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full"] }
