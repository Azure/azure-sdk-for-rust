// Copyright (c) Microsoft Corporation. All rights reserved.
//
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::generated::clients::blob_append_blob_client::BlobAppendBlobClient;
use crate::generated::clients::blob_blob_client::BlobBlobClient;
use crate::generated::clients::blob_block_blob_client::BlobBlockBlobClient;
use crate::generated::clients::blob_container_client::BlobContainerClient;
use crate::generated::clients::blob_page_blob_client::BlobPageBlobClient;
use crate::generated::clients::blob_service_client::BlobServiceClient;
use azure_core::credentials::TokenCredential;
use azure_core::{BearerTokenCredentialPolicy, ClientOptions, Pipeline, Policy, Result, Url};
use std::sync::Arc;
use typespec_client_core::fmt::SafeDebug;

pub struct BlobClient {
    container_name: String,
    endpoint: Url,
    pipeline: Pipeline,
    version: String,
}

/// Options used when creating a [`BlobClient`](crate::BlobClient)
#[derive(Clone, SafeDebug)]
pub struct BlobClientOptions {
    pub client_options: ClientOptions,
    pub version: String,
}

impl BlobClient {
    /// Creates a new BlobClient, using Entra ID authentication.
    ///
    /// # Arguments
    ///
    /// * `endpoint` - Service host
    /// * `credential` - An implementation of [`TokenCredential`](azure_core::credentials::TokenCredential) that can provide an
    ///   Entra ID token to use when authenticating.
    /// * `container_name` - The name of the container.
    /// * `options` - Optional configuration for the client.
    pub fn new(
        endpoint: &str,
        credential: Arc<dyn TokenCredential>,
        container_name: String,
        options: Option<BlobClientOptions>,
    ) -> Result<Self> {
        let options = options.unwrap_or_default();
        let mut endpoint = Url::parse(endpoint)?;
        endpoint.set_query(None);
        let auth_policy: Arc<dyn Policy> = Arc::new(BearerTokenCredentialPolicy::new(
            credential,
            vec!["https://storage.azure.com/.default"],
        ));
        Ok(Self {
            container_name,
            endpoint,
            version: options.version,
            pipeline: Pipeline::new(
                option_env!("CARGO_PKG_NAME"),
                option_env!("CARGO_PKG_VERSION"),
                options.client_options,
                Vec::default(),
                vec![auth_policy],
            ),
        })
    }

    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    /// Returns a new instance of BlobAppendBlobClient.
    pub fn get_blob_append_blob_client(&self) -> BlobAppendBlobClient {
        BlobAppendBlobClient {
            container_name: self.container_name.clone(),
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
            version: self.version.clone(),
        }
    }

    /// Returns a new instance of BlobBlobClient.
    pub fn get_blob_blob_client(&self) -> BlobBlobClient {
        BlobBlobClient {
            container_name: self.container_name.clone(),
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
            version: self.version.clone(),
        }
    }

    /// Returns a new instance of BlobBlockBlobClient.
    pub fn get_blob_block_blob_client(&self) -> BlobBlockBlobClient {
        BlobBlockBlobClient {
            container_name: self.container_name.clone(),
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
            version: self.version.clone(),
        }
    }

    /// Returns a new instance of BlobContainerClient.
    pub fn get_blob_container_client(&self) -> BlobContainerClient {
        BlobContainerClient {
            container_name: self.container_name.clone(),
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
            version: self.version.clone(),
        }
    }

    /// Returns a new instance of BlobPageBlobClient.
    pub fn get_blob_page_blob_client(&self) -> BlobPageBlobClient {
        BlobPageBlobClient {
            container_name: self.container_name.clone(),
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
            version: self.version.clone(),
        }
    }

    /// Returns a new instance of BlobServiceClient.
    pub fn get_blob_service_client(&self) -> BlobServiceClient {
        BlobServiceClient {
            container_name: self.container_name.clone(),
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
            version: self.version.clone(),
        }
    }
}

impl Default for BlobClientOptions {
    fn default() -> Self {
        Self {
            client_options: ClientOptions::default(),
            version: String::from("2025-01-05"),
        }
    }
}
