[package]
name = "messaging_eventhubs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
time = "0.3"
url = "2"

# Azure dependencies
azure_core = { path = "../core" }

# AMQP dependencies
fe2o3-amqp = "0.8"
serde_amqp = { version = "0.5.8", features = ["derive"] }
