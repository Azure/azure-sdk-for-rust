# NOTE: Please refer to https://aka.ms/azsdk/engsys/ci-yaml before editing this file.

trigger:
  branches:
    include:
    - main
    - hotfix/*
    - release/*
  paths:
    include:
    - sdk/typespec/

parameters:
- name: release_typespec
  displayName: 'typespec'
  type: boolean
  default: true
- name: release_release_typespecmacros
  displayName: 'typespec_macros'
  type: boolean
  default: true
- name: release_typespec_client_core
  displayName: 'typespec_client_core'
  type: boolean
  default: true

extends:
  template: /eng/pipelines/templates/stages/archetype-sdk-client.yml
  parameters:
    ServiceDirectory: typespec
    Artifacts:
    - name: typespec
      releaseInBatch: ${{ parameters.release_typespec }}
    - name: typespec_macros
      releaseInBatch: ${{ parameters.release_release_typespecmacros }}
    - name: typespec_client_core
      releaseInBatch: ${{ parameters.release_typespec_client_core }}
