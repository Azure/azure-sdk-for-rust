[package]
name = "azure_containers_containerregistry"
version = "0.0.1"
description = "This client library enables working with the Microsoft Azure Container Registry service to store and manage container images and deployment artifacts."
readme = "README.md"
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage = "https://github.com/azure/azure-sdk-for-rust"
documentation = "https://docs.rs/azure_containers_containerregistry"
categories = ["api-bindings"]

[lints]
workspace = true
