{
    "$schema": "../../schema/scenario.v1.json",
    "specVersion": "1.0",
    "id": "item.upsert-create-update",
    "title": "Upsert creates then updates one identity",
    "requirement": "Upsert returns 201 for a missing identity and 200 when replacing it, without creating a duplicate.",
    "maturity": "candidate",
    "precedents": [
        {
            "sdk": "dotnet",
            "path": "Microsoft.Azure.Cosmos/tests/Microsoft.Azure.Cosmos.EmulatorTests/CosmosItemTests.cs",
            "test": "UpsertItemTest"
        }
    ],
    "profiles": [
        "hostedEmulatorSmoke"
    ],
    "tags": [
        "prSmoke",
        "item"
    ],
    "backends": {
        "hostedEmulatorGatewayV1": {
            "applicability": "required",
            "fidelity": "full"
        },
        "hostedEmulatorGatewayV2": {
            "applicability": "required",
            "fidelity": "full",
            "requires": [
                "gatewayV2"
            ]
        },
        "azureLive": {
            "applicability": "supported",
            "fidelity": "full"
        }
    }
}
