# Azure Cosmos DB Driver for Rust

This crate provides transport, routing, and protocol handling for the Azure
Cosmos DB SDK. It is intended for internal SDK use, not direct use by
applications.

For supported application APIs, use the
[Azure Cosmos DB SDK for Rust](https://docs.rs/azure_data_cosmos/latest/azure_data_cosmos/).
