# {{ crate_name }} crate

The is an [Azure SDK for Rust](https://github.com/Azure/azure-sdk-for-rust) crate that is generated from the Azure REST API specifications listed in:

{{ readme_url }}

Code was generated for {{tags.len()}} `Tag`s. The default `Tag` is `{{default_tag.name()}}`.

{% for tag in tags %}
- `{{tag.name()}}` has {{self.operation_total(tag)}} operations from {{self.api_version_total(tag)}} API versions: {{self.api_versions(tag)}}{% endfor %}
