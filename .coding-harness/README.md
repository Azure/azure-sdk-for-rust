# .coding-harness/ — Pre-seeded for Coding Agent Harness

This directory was **pre-seeded by an upstream operator**, not generated by Phase 0/1/2 of the harness. The Coding Agent Harness orchestrator (defined in `cosmos-sdk-copilot-toolkit` on branch `feature/enhanced-coding-agent-harness`) should:

1. **Skip Phase 1 (Triage & Repro)** — `triage.json` is a stub. The work is not bug-driven; it's a pre-authored feature spec landed in PR [#4223](https://github.com/Azure/azure-sdk-for-rust/pull/4223). The `_seed_notes.phase_1_skipped: true` field flags this.
2. **Skip Phase 2 (Spec Generation)** — `spec.json` is derived from `sdk/cosmos/azure_data_cosmos_driver/docs/GATEWAY_20_SPEC.md`. The `_seed_notes.phase_2_skipped: true` field flags this. Phase 3+ should treat the spec doc as the authoritative normative text and use `spec.json` as the structured index into it.
3. **Start at Phase 3 (Code Implementation)** on this branch (`tvaron3/gateway-2.0-impl`). Read `env.json` for build/test/lint commands.

## Authoritative spec

- `sdk/cosmos/azure_data_cosmos_driver/docs/GATEWAY_20_SPEC.md` — full normative spec. Implementation MUST trace back to it (§3 gating, §4 retry, §5 phase plan, §6 test coverage matrix).

## Related specs (read-only context)

- `sdk/cosmos/azure_data_cosmos_driver/docs/TRANSPORT_PIPELINE_SPEC.md` — sharded HTTP/2 transport that Gateway 2.0 reuses verbatim.
- `sdk/cosmos/azure_data_cosmos_driver/docs/PARTITION_KEY_RANGE_CACHE_SPEC.md` — PKRange cache. Do NOT refresh on 404/1002.
- `sdk/cosmos/azure_data_cosmos_driver/docs/PARTITION_LEVEL_FAILOVER_SPEC.md` — PLF takes precedence over the prefer-remote-region hint on 404/1002.

## Branch state

- Branch: `tvaron3/gateway-2.0-impl`
- Forked from: `tvaron3/gateway-2.0-design` @ `9c7484ef3`
- Spec PR: [#4223](https://github.com/Azure/azure-sdk-for-rust/pull/4223) (still open at the time of seeding)
