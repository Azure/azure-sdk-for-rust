[graph]
all-features = true
exclude-dev = true

[advisories]
git-fetch-with-cli = true
ignore = []

[licenses]
allow = [
  # See https://docs.opensource.microsoft.com/legal/resources/oss-licenses-by-type for acceptable licenses.
  "Apache-2.0",
  "BSD-3-Clause",
  "CDLA-Permissive-2.0",
  "ISC",
  "MIT",
  # MPL-2.0 is allowed because `cbindgen` (a build-only dependency of
  # `azure_data_cosmos_driver_native`) is MPL-2.0 licensed.
  "MPL-2.0",
  "Unicode-3.0",
  "Zlib",
]

[[licenses.clarify]]
name = "ring"
expression = "MIT AND ISC AND OpenSSL"
license-files = [
  { path = "LICENSE", hash = 0xbd0eed23 },
]

[bans]
multiple-versions = "allow"
wildcards = "deny"
allow-wildcard-paths = true
deny = [
  "ring",
  "smol",
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
